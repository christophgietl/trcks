[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core>=3.2,<4"]

[project]
dependencies = []
description = "Add your description here"
name = "trcks"
readme = "README.md"
requires-python = ">=3.9"
version = "0.1.0"
